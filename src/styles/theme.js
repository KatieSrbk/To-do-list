import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  styles: {
    global: {
      body: {
        bg: 'rgb(186,143,255)',
        color: 'black',
      },
    },
  },
})

export default theme
